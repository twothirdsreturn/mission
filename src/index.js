console.log('Hello, World!');
import React from "react";
import ReactDOM from "react-dom";
import App from "./components/App"; // Ensure App.js loads everything

ReactDOM.render(<App />, document.getElementById("root"));
